import React from 'react';
import {
  SafeAreaView,
  ScrollView,
  Text,
  View,
} from 'react-native';

function AboutScreen(): JSX.Element {

  return (
    <SafeAreaView>
        <Text> about </Text>
    </SafeAreaView>
  );
}

export default AboutScreen;
