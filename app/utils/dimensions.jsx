import {Dimensions} from 'react-native';

const {
  fontScale: phoneFontScale,
  height: phoneHeight,
  scale: phoneScale,
  width: phoneWidth,
} = Dimensions.get('window');

export {phoneFontScale, phoneHeight, phoneScale, phoneWidth};
